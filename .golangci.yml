run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - errcheck
    - godot
    - staticcheck

linters-settings:
  errcheck:
    check-blank: false
    check-type-assertions: false

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-use-default: false
